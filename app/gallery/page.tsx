import Gallery from '@/components/Gallery'

export const metadata = {
  title: 'Gallery — Pick Me Up Affair',
  description: 'Each one, a story. From intimate gifts to grand event centrepieces.',
}

export default function GalleryPage() {
  return <Gallery standalone />
}
